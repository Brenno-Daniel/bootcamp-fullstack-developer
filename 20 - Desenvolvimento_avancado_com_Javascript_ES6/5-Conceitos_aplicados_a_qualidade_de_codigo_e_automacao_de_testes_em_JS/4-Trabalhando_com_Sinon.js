// Sinon

const { normalize } = require("path")

// Instalar: npm i --save-dev sinon
// Com sinon podemos mochar funções e observar se foram invocadas

// Podemos criar funções espiãs com o sinon.spy()
// Com espiões conseguimos verificar se a função foi invocada da maneira correta
// Podemos utilizar o spy em funções já contruídas

// stub = substituir o métodos e colocar retornos customizados

// restore = retorna o método ao normalize.

